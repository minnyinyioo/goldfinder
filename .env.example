# Public production origin with no path or trailing slash.
# Replace after the custom domain is attached and its TLS certificate is active.
NEXT_PUBLIC_SITE_URL=https://goldfinder.vercel.app

# Server-only Telegram Bot API credentials. Never prefix these with NEXT_PUBLIC_.
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
