"use client";import {ChangeEvent,useEffect,useMemo,useRef,useState} from "react";import {Archive,CheckCircle2,Download,FileCheck2,Image as ImageIcon,MapPin,RefreshCw,ShieldAlert,Trash2,Upload} from "lucide-react";import "./data-vault.css";
type Lang="zh"|"en";type Sample=Record<string,unknown>&{id?:string;sample?:string;lat?:string;lng?:string;accuracy?:string};type Mode="exact"|"rounded"|"none";type Restore="merge"|"replace";
const tx={zh:{title:"本机数据保险库",lead:"把样品数据库与取样计划保存为带版本标记的备份。导出前可移除或降低精确坐标；恢复前会先验证格式。",samples:"样品记录",plan:"取样计划",photos:"现场照片",local:"仅保存在当前浏览器",photoNote:"照片存于独立的浏览器图片库，不会自动放入 JSON。请在现场记录页逐张下载原图，并与本备份一起保管。",privacy:"坐标隐私",exact:"保留精确坐标",rounded:"约化到小数点后 2 位",none:"删除全部坐标",roundedNote:"约化坐标仅用于降低公开分享风险，并不构成强匿名化；稀有地点仍可能被推断。",export:"下载完整备份",restore:"恢复备份",method:"恢复方式",merge:"合并并跳过重复记录",replace:"替换当前样品和计划",choose:"选择备份 JSON",invalid:"文件不是有效的 Goldfinder 备份，未作任何更改。",success:"恢复完成",confirmReplace:"替换将覆盖当前样品记录和取样计划。确定继续？",clear:"清除项目数据",clearLead:"仅清除样品记录和取样计划；不会删除照片或主题设置。",type:"输入 DELETE 确认",clearButton:"永久清除本机项目数据",cleared:"项目数据已清除。",manifest:"备份内容",schema:"格式版本",generated:"生成时间",coords:"坐标处理",noPlan:"尚无已保存计划"},en:{title:"Device data vault",lead:"Save the sample database and sampling plan in a versioned backup. Remove or reduce coordinate precision before exporting; every restore is validated first.",samples:"Sample records",plan:"Sampling plan",photos:"Field photographs",local:"Stored in this browser only",photoNote:"Photographs use a separate browser image store and are not embedded in JSON. Download originals from Field Records and keep them beside this backup.",privacy:"Coordinate privacy",exact:"Keep exact coordinates",rounded:"Round to 2 decimal places",none:"Remove all coordinates",roundedNote:"Rounding lowers casual sharing risk but is not strong anonymisation; a rare location may still be inferred.",export:"Download full backup",restore:"Restore backup",method:"Restore method",merge:"Merge and skip duplicate records",replace:"Replace current samples and plan",choose:"Choose backup JSON",invalid:"This is not a valid Goldfinder backup. No changes were made.",success:"Restore complete",confirmReplace:"Replace will overwrite current samples and sampling plan. Continue?",clear:"Clear project data",clearLead:"Clears samples and sampling plan only; photographs and theme settings are retained.",type:"Type DELETE to confirm",clearButton:"Permanently clear device project data",cleared:"Project data cleared.",manifest:"Backup contents",schema:"Schema version",generated:"Generated",coords:"Coordinate handling",noPlan:"No saved plan"}} as const;
export default function DataVault({lang}:{lang:Lang}){const c=tx[lang],[samples,setSamples]=useState<Sample[]>([]),[plan,setPlan]=useState<unknown>(null),[mode,setMode]=useState<Mode>("none"),[restoreMode,setRestoreMode]=useState<Restore>("merge"),[phrase,setPhrase]=useState(""),[message,setMessage]=useState(""),file=useRef<HTMLInputElement>(null);function refresh(){try{const rows=JSON.parse(localStorage.getItem("goldfinder-samples-v2")||"[]");setSamples(Array.isArray(rows)?rows:[])}catch{setSamples([])}try{setPlan(JSON.parse(localStorage.getItem("goldfinder-sampling-plan")||"null"))}catch{setPlan(null)}}useEffect(refresh,[]);const located=useMemo(()=>samples.filter(x=>x.lat!==""&&x.lng!==""&&Number.isFinite(Number(x.lat))&&Number.isFinite(Number(x.lng))).length,[samples]);function protectedSamples(){return samples.map(x=>{const row={...x};if(mode==="none"){delete row.lat;delete row.lng;delete row.accuracy}else if(mode==="rounded"){if(Number.isFinite(Number(row.lat)))row.lat=Number(row.lat).toFixed(2);if(Number.isFinite(Number(row.lng)))row.lng=Number(row.lng).toFixed(2);delete row.accuracy}return row})}function download(){const payload={schema:"goldfinder.vault",version:1,exportedAt:new Date().toISOString(),coordinateMode:mode,contents:{samples:protectedSamples(),samplingPlan:plan}};const url=URL.createObjectURL(new Blob([JSON.stringify(payload,null,2)],{type:"application/json"})),a=document.createElement("a");a.href=url;a.download=`goldfinder-vault-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(url)}async function restore(e:ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0];if(!f)return;try{const d=JSON.parse(await f.text());if(d.schema!=="goldfinder.vault"||d.version!==1||!d.contents||!Array.isArray(d.contents.samples))throw new Error();if(restoreMode==="replace"&&!confirm(c.confirmReplace))return;let next=d.contents.samples as Sample[];if(restoreMode==="merge"){const keys=new Set(samples.map(x=>String(x.id||x.sample||"")));next=[...samples,...next.filter(x=>!keys.has(String(x.id||x.sample||"")))]}localStorage.setItem("goldfinder-samples-v2",JSON.stringify(next));if(d.contents.samplingPlan)localStorage.setItem("goldfinder-sampling-plan",JSON.stringify(d.contents.samplingPlan));else if(restoreMode==="replace")localStorage.removeItem("goldfinder-sampling-plan");refresh();setMessage(`${c.success}: ${next.length} ${c.samples}`)}catch{setMessage(c.invalid)}finally{e.target.value=""}}function clear(){if(phrase!=="DELETE")return;localStorage.removeItem("goldfinder-samples-v2");localStorage.removeItem("goldfinder-sampling-plan");setPhrase("");refresh();setMessage(c.cleared)}return <section className="vault"><header><p className="eyebrow">LOCAL-FIRST DATA PROTECTION</p><h1><Archive size={34}/>{c.title}</h1><p>{c.lead}</p></header><div className="vault-metrics"><Metric icon={FileCheck2} value={samples.length} label={c.samples}/><Metric icon={MapPin} value={located} label={lang==="zh"?"含坐标样品":"Located samples"}/><Metric icon={CheckCircle2} value={plan?1:0} label={plan?c.plan:c.noPlan}/><Metric icon={ImageIcon} value="—" label={c.photos}/></div><div className="vault-grid"><article><h2><Download/>{c.export}</h2><div className="vault-manifest"><b>{c.manifest}</b><span>{c.samples}: {samples.length}</span><span className="vault-plan-state">{c.plan}: {plan?<><CheckCircle2 size={15}/>{lang==="zh"?"已保存":"Saved"}</>:"—"}</span><span>{c.schema}: goldfinder.vault / 1</span><span>{c.generated}: {new Date().toLocaleDateString(lang==="zh"?"zh-CN":"en-GB")}</span></div><fieldset><legend><ShieldAlert size={17}/>{c.privacy}</legend>{(["none","rounded","exact"] as Mode[]).map(x=><label key={x}><input type="radio" name="coordinate-mode" checked={mode===x} onChange={()=>setMode(x)}/><span>{c[x]}</span></label>)}</fieldset><p className="vault-hint">{c.roundedNote}</p><button className="vault-primary" onClick={download} disabled={!samples.length&&!plan}><Download size={17}/>{c.export}</button></article><article><h2><Upload/>{c.restore}</h2><fieldset><legend><RefreshCw size={17}/>{c.method}</legend>{(["merge","replace"] as Restore[]).map(x=><label key={x}><input type="radio" name="restore-mode" checked={restoreMode===x} onChange={()=>setRestoreMode(x)}/><span>{c[x]}</span></label>)}</fieldset><button className="vault-secondary" onClick={()=>file.current?.click()}><Upload size={17}/>{c.choose}</button><input ref={file} hidden type="file" accept="application/json" onChange={restore}/><div className="vault-photo-note"><ImageIcon size={19}/><p>{c.photoNote}</p></div></article></div>{message&&<p className="vault-message" role="status">{message}</p>}<section className="vault-danger"><div><h2><Trash2 size={20}/>{c.clear}</h2><p>{c.clearLead}</p></div><label>{c.type}<input value={phrase} onChange={e=>setPhrase(e.target.value)} placeholder="DELETE"/></label><button disabled={phrase!=="DELETE"} onClick={clear}><Trash2 size={16}/>{c.clearButton}</button></section><p className="vault-local">{c.local}</p></section>}
function Metric({icon:Icon,value,label}:{icon:typeof Archive;value:string|number;label:string}){return <article><Icon size={20}/><strong>{value}</strong><span>{label}</span></article>}
