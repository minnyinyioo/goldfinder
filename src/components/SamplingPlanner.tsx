"use client";import {useMemo,useState} from "react";import "./sampling-planner.css";
const text={zh:{eyebrow:'SAMPLING DESIGN TOOL',title:'取样规划与工作量',lead:'把目标范围转换为规则样线、分层样和 QA/QC 数量。间距和样品体积必须由地质变化、粒径和先导试验决定。',objective:'工作目标',material:'主要土质/材料',length:'沿矿化或河段长度（m）',spacing:'样线/栅栏间距（m）',points:'每条样线主样点数',layers:'每点垂向分层数',volume:'每个主样体积（L）',dup:'现场重复样（%）',blank:'空白样（%）',crm:'标准样 CRM（%）',result:'计划工作量',fences:'样线数量',primary:'主样数量',duplicates:'重复样',blanks:'空白样',standards:'标准样',bags:'总样袋/记录',total:'主样总处理体积',save:'保存计划到本机',export:'导出计划 JSON',warning:'这些数字是布置与工作量计算，不是统计充分性证明。应先做定向样和累计体积试验，再由合资格人员根据品位方差、粗金粒径、地质连续性和目标精度调整。'},en:{eyebrow:'SAMPLING DESIGN TOOL',title:'Sampling layout and workload',lead:'Convert a target area into systematic fences, vertical intervals, and QA/QC counts. Spacing and sample support must follow geological variability, particle size, and pilot work.',objective:'Study objective',material:'Principal soil/material',length:'Length along target or stream reach (m)',spacing:'Fence / traverse spacing (m)',points:'Primary points per fence',layers:'Vertical intervals per point',volume:'Volume per primary sample (L)',dup:'Field duplicates (%)',blank:'Blanks (%)',crm:'Certified reference materials (%)',result:'Planned workload',fences:'Number of fences',primary:'Primary samples',duplicates:'Field duplicates',blanks:'Blanks',standards:'Reference materials',bags:'Total bags / records',total:'Total primary volume',save:'Save plan on this device',export:'Export plan JSON',warning:'These are layout and workload calculations, not proof of statistical sufficiency. Use orientation and progressive-volume tests, then have a qualified person adjust the design for grade variance, coarse-gold size, geological continuity, and target precision.'}};
const guidance:Record<string,{zh:string;en:string}>={'Fine sand / silt':{zh:'在同一沉积单元内做等体积组合样，避免只取表面黑砂条带。',en:'Use equal-support composites within one depositional unit; do not sample only visible black-sand streaks.'},'Sand and gravel':{zh:'记录最大粒径和筛上物，区分原位与挖松体积。',en:'Record top size and oversize; distinguish bank from loose volume.'},'Cobble-rich gravel':{zh:'小淘金盘通常不具代表性，应以试坑/沟槽覆盖完整粒级。',en:'Small pans are rarely representative; use pits or trenches that include the full size distribution.'},'Clay false bottom':{zh:'接触面上方、黏土表面和裂缝材料分层编号，不要混成一个样。',en:'Number the overlying interval, clay surface, and crack material separately.'},'Residual soil':{zh:'按坡位和土层布网，使用质量品位时保留实测干容重。',en:'Stratify the grid by slope position and soil horizon; retain measured dry bulk density.'}};
export default function SamplingPlanner({lang='zh'}:{lang?:'zh'|'en'}){const c=text[lang];const [objective,setObjective]=useState('Reconnaissance');const [material,setMaterial]=useState('Sand and gravel');const [length,setLength]=useState(200);const [spacing,setSpacing]=useState(25);const [points,setPoints]=useState(5);const [layers,setLayers]=useState(2);const [volume,setVolume]=useState(50);const [dup,setDup]=useState(10);const [blank,setBlank]=useState(5);const [crm,setCrm]=useState(5);const r=useMemo(()=>{const fences=Math.max(1,Math.floor(length/Math.max(1,spacing))+1);const primary=fences*Math.max(1,points)*Math.max(1,layers);const duplicates=Math.ceil(primary*Math.max(0,dup)/100);const blanks=Math.ceil(primary*Math.max(0,blank)/100);const standards=Math.ceil(primary*Math.max(0,crm)/100);return{fences,primary,duplicates,blanks,standards,bags:primary+duplicates+blanks+standards,total:primary*Math.max(0,volume)/1000}},[length,spacing,points,layers,volume,dup,blank,crm]);const plan={objective,material,length,spacing,points,layers,volume,dup,blank,crm,...r};function save(){localStorage.setItem('goldfinder-sampling-plan',JSON.stringify({...plan,savedAt:new Date().toISOString()}));alert(lang==='zh'?'计划已保存在本机。':'Plan saved on this device.')}function download(){const url=URL.createObjectURL(new Blob([JSON.stringify({...plan,createdAt:new Date().toISOString()},null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download='goldfinder-sampling-plan.json';a.click();URL.revokeObjectURL(url)}const num=(label:string,val:number,set:(v:number)=>void)=><label>{label}<input type="number" min="0" value={val} onChange={e=>set(Number(e.target.value))}/></label>;return <section className="planner"><div className="planner-head"><p className="eyebrow">{c.eyebrow}</p><h2>{c.title}</h2><p>{c.lead}</p></div><div className="planner-grid"><div className="planner-inputs"><label>{c.objective}<select value={objective} onChange={e=>setObjective(e.target.value)}><option>Reconnaissance</option><option>Delineation</option><option>Grade control</option></select></label><label>{c.material}<select value={material} onChange={e=>setMaterial(e.target.value)}>{Object.keys(guidance).map(x=><option key={x}>{x}</option>)}</select></label>{num(c.length,length,setLength)}{num(c.spacing,spacing,setSpacing)}{num(c.points,points,setPoints)}{num(c.layers,layers,setLayers)}{num(c.volume,volume,setVolume)}{num(c.dup,dup,setDup)}{num(c.blank,blank,setBlank)}{num(c.crm,crm,setCrm)}</div><div className="planner-results"><h3>{c.result}</h3>{([[c.fences,r.fences],[c.primary,r.primary],[c.duplicates,r.duplicates],[c.blanks,r.blanks],[c.standards,r.standards],[c.bags,r.bags],[c.total,`${r.total.toFixed(2)} m³`]] as [string,string|number][]).map(([a,b])=><div className="plan-result" key={a}><span>{a}</span><strong>{b}</strong></div>)}<div className="material-note"><b>{material}</b><p>{guidance[material][lang]}</p></div><div className="actions"><button className="button" onClick={save}>{c.save}</button><button className="button secondary" onClick={download}>{c.export}</button></div><div className="notice">{c.warning}</div></div></div></section>}
