import Image from "next/image";import {atlasItems} from "@/data/media";import "./atlas.css";
export default function Atlas(){return <><div className="page-head"><p className="eyebrow">VERIFIED PHOTO ATLAS</p><h1>真实图片对照图鉴</h1><p className="lead">图片用于学习“应该观察什么”，不是用照片直接鉴定样品。每张图片均附原始页面、作者和许可。</p></div><section className="section"><div className="compare"><div><strong>先看形态</strong><p>晶形、片状、粒状、延展或脆裂</p></div><div><strong>再做测试</strong><p>磁性、条痕、硬度、密度与解理</p></div><div><strong>最后验证</strong><p>代表性取样与适用的实验室分析</p></div></div><div className="atlas-grid">{atlasItems.map(x=><article className="card atlas-card" id={x.slug} key={x.slug}><Image src={x.image} alt={x.alt} width={960} height={720}/><div className="atlas-body"><b>现场参照</b><h2>{x.name}</h2><p><strong>观察重点：</strong>{x.observe}</p><div className="notice"><strong>不能据此推断：</strong>{x.notProof}</div><p className="atlas-meta">图片：{x.author} · {x.license} · <a href={x.source} target="_blank" rel="noreferrer">查看原始图片与许可</a></p></div></article>)}</div></section></>}
